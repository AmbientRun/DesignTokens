[package]
name = "ambient_design_tokens_css"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[build-dependencies]
ambient_design_tokens_core = { path = "../core" }
